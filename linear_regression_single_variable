import pandas as pd
import numpy as np
import matplotlib.pylot as plt
from sklearn import linear_modelimport matplotlib.pyplot as plt
from sklearn import linear_modeldf = pd.read_csv("data.csv")
df = pd.read_csv("homeprices.csv")
df = pd.read_csv("FIFA19.csv")
df
%matplotlib inline
plt.scatter(df.area,df.price)
%matplotlib inline
plt.xlabel('area')
plt.ylabel('priceUS')
plt.scatter(df.Age,df.Overall, color='red',marker='+')
reg.coef_
reg.intercept_
d = pd.read_csv('FIFA19.csv')
d.head(3)
reg.predict(d)
d['Kids'] =p 
d.to_csv('prediction.csv')
reg = linear_model.LinearRegression()
reg.fit(df[['Overall']],df.Age)
import pickle
with open('model_pickle','wb') as f:
      pickle.dump(model, f)
with open('model_pickle','rb')
   mp = pickle.load(f)
mp.predict(5000)
from sklearn.externals import joblib
joblib.dump(model, 'model_joblib') #save the mofel/ formulas to use later
mj=joblib.load('model_joblib')  #use model


